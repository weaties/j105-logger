<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  com.helmlog.backup.plist — launchd agent for daily Pi-to-Mac backup.
  Installed by scripts/setup-backup-mac.sh into ~/Library/LaunchAgents/.

  Runs backup.sh every day at 03:00 local time.
  Logs stdout+stderr to ~/backups/helmlog/backup.log.

  Manual control (after installation):
    launchctl start  com.helmlog.backup   # run now
    launchctl stop   com.helmlog.backup   # cancel running job
    launchctl unload ~/Library/LaunchAgents/com.helmlog.backup.plist  # disable
    launchctl load   ~/Library/LaunchAgents/com.helmlog.backup.plist  # re-enable
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.helmlog.backup</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>BACKUP_SCRIPT_PATH</string>
  </array>

  <!-- Daily at 03:00 local time -->
  <key>StartCalendarInterval</key>
  <dict>
    <key>Hour</key>
    <integer>3</integer>
    <key>Minute</key>
    <integer>0</integer>
  </dict>

  <!-- Log file — directory must exist before launchd writes to it -->
  <key>StandardOutPath</key>
  <string>BACKUP_LOG_PATH</string>
  <key>StandardErrorPath</key>
  <string>BACKUP_LOG_PATH</string>

  <!-- Run even if the scheduled time was missed (e.g. Mac was asleep) -->
  <key>RunAtLoad</key>
  <false/>

  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    <key>HOME</key>
    <string>HOME_PATH</string>
    <key>PI</key>
    <string>PI_TARGET</string>
    <key>SSH_KEY</key>
    <string>SSH_KEY_PATH</string>
    <key>REPORT_TO</key>
    <string>REPORT_TO_ADDR</string>
  </dict>
</dict>
</plist>
